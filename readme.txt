 - Simple Hello World Application that will connect to a database and return values.

-- using Python as exploring the language 
-- using Docker as exploring options and is part of the requirements

run via:
docker-compose up

to clean:
docker-compose rm -f 
docker-compose up --build -d
